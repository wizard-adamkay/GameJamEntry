/// @description Insert description here
// You can write your code in this editor
if(iFrame <= 0){
	show_debug_message(abs(other.hsp) + abs(other.vsp))
	health -= abs(other.hsp) + abs(other.vsp);
	iFrame = 60;
	flash = 3;
} 